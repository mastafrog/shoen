CREATE TABLE tours (
    `id` INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(100) NOT NULL,
    short_desc VARCHAR(255) NOT NULL
) ENGINE=InnoDB;


inset into tours 
        (title, short_desc) 
    values 
        ('Kurzer Titel', 'Super langer Titel');